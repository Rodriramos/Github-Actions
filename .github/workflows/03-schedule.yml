name: 03 - Schedule

on:
  schedule:
    - cron: '0 9 * * *'
  
jobs:
  scheduled:
    runs-on: ubuntu-24.04
    steps:
      - name: Saludar en modo programado
        run: echo "¡Hola! Este workflow se ejecuta automáticamente según el cron programado."
